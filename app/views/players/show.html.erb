<h3>Vitals</h3>
<p>
  <b>Name:</b>
  <%= @player.name %>
  <br/>
  <b>Nickname:</b>
  <%= @player.nickname %>
  <br/>
  <b>Points:</b>
  <%= @player.points %>
  <br/>
  <b>Bonus Points:</b>
  <%= @player.bonus_points %>
</p>
<h3>Game History</h3>
<table class="table table-striped table-dark table-bordered table-sm" border="1">
  <thead><tr><th>Date</th><th>Place</th><th>Points</th><th>Bonus</th></tr></thead>
  <tbody>
<% @entries.each do |entry| -%>
  <tr><td><%=entry.game.created_at.to_date.to_formatted_s(:short)%></td><td><%=entry.final_position%><%=entry.final_position ? entry.final_position.ordinal : -1%> of <%=entry.game.entrants.size%></td><td><%=entry.score%></td><td><%=entry.bonus_score%></tr>
<% end %>
  </tbody>
</table>
<p><%=will_paginate @entries %></p>
<p><%=page_entries_info(@entries).html_safe%></p>

<%= link_to 'Edit', edit_player_path(@player) %> |
<%= link_to 'Back', players_path %>
